class Solution {
    boolean twoSum(int arr[], int target) {
        HashSet<Integer> set=new HashSet<>();
        for(int x:arr){
            int needed=target-x;
            if(set.contains(needed)){
                return true;
            }
            set.add(x);
        }
        return false;
    }
}